<Page
    x:Class="FlipViewContextPreview01.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlipViewContextPreview01"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <FlipView x:Name="NormalFlipView"
                  Grid.Row="0"
                  Grid.Column="0"
                  >
            
        </FlipView>
        <FlipView x:Name="WorkingFlipView"
                  Grid.Row="0"
                  Grid.Column="1"
                  >

        </FlipView>
        <FlipView x:Name="ContextFlipView"
                  Grid.Row="1"
                  Grid.Column="0"
                  >

        </FlipView>
        <FlipView x:Name="PreviewFlipView"
                  Grid.Row="1"
                  Grid.Column="1"
                  >

        </FlipView>
    </Grid>
</Page>
